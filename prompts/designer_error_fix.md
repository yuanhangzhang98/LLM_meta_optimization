## Error
```
{error_message}
{full_traceback}
```

## Task
Fix the error in your implementation components and return all {num_components} corrected components: {component_names}.

## Output Format
Return a JSON object with:
```json
{{
  "error_summary": "What went wrong and how you fixed it",
  "solver_code": "Complete Python code with imports and corrected components"
}}
```