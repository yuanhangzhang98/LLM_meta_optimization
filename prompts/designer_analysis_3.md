## Task
Decide whether to promote fidelity or conclude the experiment.

Guidelines:
- Promote if design performs well and current schedule cannot fully evaluate its potential
- Conclude if design performs poorly or current schedule already sufficiently evaluates it

## Output Format
Return a JSON object with:
```json
{{
  "reasoning": "<brief explanation of your decision>",
  "promote_fidelity": true|false
}}
```
