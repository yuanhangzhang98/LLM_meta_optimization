# HEBO Requirements
# CRITICAL: HEBO requires numpy<1.25 (numpy 2.x breaks it)
# CRITICAL: HEBO requires Python <=3.10
# These versions match the working local conda environment (hebo)

# Core dependencies
numpy>=1.24.0,<1.25    # HEBO requires <1.25, breaks with numpy 2.x
scipy>=1.10.0,<2.0
pydantic>=2.0.0
torch>=2.9.0

# HEBO and its pinned dependencies
HEBO==0.3.6
pymoo==0.6.0           # HEBO requires exactly 0.6.0
gpytorch>=1.14.0
GPy>=1.13.0
catboost>=1.2.0

# OpenAI
openai>=1.0.0